# M9 — MCP Adapter

**Milestone:** M9  
**Effort:** 2 ngày  
**Prerequisite:** M7  
**Spec:** `13_CLI_SPECIFICATION.md §10`

---

## Objective

Expose PlatformService qua MCP protocol. AI tools (Cursor, Claude Code, Kiro) có thể gọi Harness thông qua MCP tools. `harness proposal approve` **không được expose** qua MCP.

---

## Vertical Slice

```bash
# Start MCP server
harness mcp-server
# → MCP server listening on stdio

# AI tool gọi MCP tool
# Tool: harness_run
# Input: { "description": "list all TODO comments" }
# Output: { "status": "COMPLETED", "results": [...] }
```

---

## Package Structure

```
src/adapters/mcp/
  server.ts             ← T9.1
  tools/
    run.ts              ← T9.2
    validate.ts
    proposal_list.ts
    proposal_submit.ts
  formatter/
    McpFormatter.ts     ← T9.3
```

---

## Tasks

### T9.1 — MCP Server

**File:** `src/adapters/mcp/server.ts`

```typescript
import { Server } from '@modelcontextprotocol/sdk/server/index.js'
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js'

export async function startMcpServer(platform: PlatformService): Promise<void> {
  const server = new Server(
    { name: 'harness', version: VERSION },
    { capabilities: { tools: {} } }
  )

  // Register tools
  server.setRequestHandler(ListToolsRequestSchema, async () => ({
    tools: [runTool, validateTool, proposalListTool, proposalSubmitTool]
  }))

  server.setRequestHandler(CallToolRequestSchema, async (request) => {
    return handleToolCall(request, platform)
  })

  const transport = new StdioServerTransport()
  await server.connect(transport)
}
```

---

### T9.2 — 4 MCP Tools

**Không expose `approve`** — đây là human-only action, chỉ qua CLI.

**Tool: `harness_run`**
```typescript
{
  name: 'harness_run',
  description: 'Execute a task using Harness Platform',
  inputSchema: {
    type: 'object',
    properties: {
      description: { type: 'string', description: 'Task description' },
      workflowId: { type: 'string', description: 'Optional workflow ID' },
    },
    required: ['description'],
  }
}
// Handler: platform.run({ description, workflowId })
// Return: { content: [{ type: 'text', text: JSON.stringify(result) }] }
```

**Tool: `harness_validate`**
```typescript
{
  name: 'harness_validate',
  description: 'Validate repository structure',
  inputSchema: {
    type: 'object',
    properties: { root: { type: 'string' } },
  }
}
// Handler: platform.validate(root ?? process.cwd())
```

**Tool: `harness_proposal_list`**
```typescript
{
  name: 'harness_proposal_list',
  description: 'List knowledge proposals',
  inputSchema: {
    type: 'object',
    properties: {
      status: { type: 'string', enum: ['DRAFT','SUBMITTED','REVIEWING','APPROVED','REJECTED','PROMOTED'] },
    }
  }
}
// Handler: platform.listProposals({ status })
```

**Tool: `harness_proposal_submit`**
```typescript
{
  name: 'harness_proposal_submit',
  description: 'Submit a proposal for review',
  inputSchema: {
    type: 'object',
    properties: { id: { type: 'string' } },
    required: ['id'],
  }
}
// Handler: platform.submitProposal({ id })
```

---

### T9.3 — MCP Formatter

```typescript
export class McpFormatter {
  toToolResult(data: unknown, isError = false): CallToolResult {
    return {
      content: [{ type: 'text', text: JSON.stringify(data, null, 2) }],
      isError,
    }
  }

  toErrorResult(error: HarnessError): CallToolResult {
    return this.toToolResult({
      error: { code: error.code, message: error.message, retryable: error.retryable }
    }, true)
  }
}
```

---

### T9.4 — CLI command: mcp-server

**File:** `src/adapters/cli/commands/mcp-server.ts`

```bash
harness mcp-server [--port <port>]
```

Khởi động MCP server trên stdio (default) hoặc port.

---

## Acceptance Scenarios — M9

**Scenario 1 — Tool list:**
```json
// MCP ListTools request
// Expected: 4 tools (run, validate, proposal_list, proposal_submit)
// NOT: harness_proposal_approve
```

**Scenario 2 — harness_run via MCP:**
```json
// Input: {"description": "list files in src/"}
// Expected: { "content": [{ "type": "text", "text": "{...result...}" }] }
```

**Scenario 3 — Error handling:**
```json
// Input: {} (missing description)
// Expected: isError: true, error code in response
```

**Scenario 4 — Approve not available:**
```
// MCP client tries to call harness_proposal_approve
// Expected: tool not found error (not registered)
```

---

## Definition of Done — M9

- [ ] 4 MCP tools registered và hoạt động
- [ ] `harness_proposal_approve` không tồn tại trong MCP
- [ ] Error responses theo MCP format
- [ ] `harness mcp-server` khởi động không lỗi
- [ ] MCP adapter không import gì ngoài `platform`

## Review Gate — M9

```
AI self-review: approve tool absent from MCP
      ↓
tsc + eslint + dependency-cruiser (0 errors)
      ↓
Integration tests pass (mock MCP client)
      ↓
Human review
      ↓
Merge to main
```

---

---

# M10 — Conformance Suite

**Milestone:** M10  
**Effort:** 3 ngày  
**Prerequisite:** M7 + M8 + M9  
**Spec:** `17_CONFORMANCE.md`

---

## Objective

Implement 30 conformance test cases từ spec. Output `conformance-report.json` đạt Level 2.

---

## Vertical Slice

```bash
harness conformance run
# Output:
# Running 30 conformance tests...
# TC-01 PASS  Parse valid minimal manifest
# TC-02 PASS  Ignore unknown fields
# TC-03 PASS  MFT_003 on missing required field
# ...
# TC-30 PASS  harness version returns string
#
# Results: 30/30 passed (0 failed, 0 skipped)
# Compliance Level: Level 2
# Report: conformance-report.json
```

---

## Package Structure

```
tests/conformance/
  runner.ts                    ← test runner
  report.ts                    ← report generator
  groups/
    tc01-05-manifest.test.ts   ← Group 1
    tc06-10-assets.test.ts     ← Group 2
    tc11-14-context.test.ts    ← Group 3
    tc15-18-execution.test.ts  ← Group 4
    tc19-22-capability.test.ts ← Group 5
    tc23-26-governance.test.ts ← Group 6
    tc27-30-cli.test.ts        ← Group 7
```

---

## 30 Test Cases

Implement từng test case từ `17_CONFORMANCE.md §6`:

**Group 1 — Repository & Manifest (TC-01 đến TC-05)**

| ID | Test | Expected |
|----|------|----------|
| TC-01 | Parse valid minimal manifest | success, Manifest object returned |
| TC-02 | Parse manifest with unknown fields | MUST ignore unknown fields |
| TC-03 | Parse manifest missing required field | throw MFT_003 |
| TC-04 | Parse manifest wrong version field | throw MFT_004 |
| TC-05 | Discover repository from subdirectory | correct root returned |

**Group 2 — Asset Tests (TC-06 đến TC-10)**

| ID | Test | Expected |
|----|------|----------|
| TC-06 | Load rule with valid front matter | Rule object returned |
| TC-07 | Load asset missing `id` field | throw REPO_007 |
| TC-08 | Load two assets with same ID | throw REPO_006 |
| TC-09 | Override: Local overrides Shared (same id) | Local version in result |
| TC-10 | Merge: Knowledge from both sources | both entries in collection |

**Group 3 — Context Tests (TC-11 đến TC-14)**

| ID | Test | Expected |
|----|------|----------|
| TC-11 | Build context from valid repository | RuntimeContext returned |
| TC-12 | Filter by scope_paths matching working dir | only scoped rules kept |
| TC-13 | priority_trim when over budget | items trimmed, fits budget |
| TC-14 | Cache hit on second build with same key | no rebuild (cache hit) |

**Group 4 — Execution Tests (TC-15 đến TC-18)**

| ID | Test | Expected |
|----|------|----------|
| TC-15 | Execute task with valid workflow | status = COMPLETED |
| TC-16 | Step timeout → retry → EXEC_004 | max retries exceeded, FAILED |
| TC-17 | Cancel task midway | status = CANCELLED |
| TC-18 | RuntimeContext immutable during execution | no mutation possible |

**Group 5 — Capability Tests (TC-19 đến TC-22)**

| ID | Test | Expected |
|----|------|----------|
| TC-19 | Invoke `harness.file.read` valid path | file content returned |
| TC-20 | Invoke unregistered capability | throw CAP_001 |
| TC-21 | Invoke capability with invalid input | throw CAP_002 |
| TC-22 | Invoke without required permission | throw CAP_004 |

**Group 6 — Governance Tests (TC-23 đến TC-26)**

| ID | Test | Expected |
|----|------|----------|
| TC-23 | Create proposal | status = DRAFT |
| TC-24 | Submit proposal without evidence | throw GOV_004 |
| TC-25 | AI agent attempts approve via MCP | tool not found / permission denied |
| TC-26 | Human approves | status = APPROVED |

**Group 7 — CLI Tests (TC-27 đến TC-30)**

| ID | Test | Expected |
|----|------|----------|
| TC-27 | `harness doctor` healthy | exit 0 |
| TC-28 | `harness validate` invalid repo | exit 2 |
| TC-29 | `harness run <task>` success | exit 0 |
| TC-30 | `harness version` | version string, exit 0 |

---

## Conformance Report

Output `conformance-report.json` theo schema `17_CONFORMANCE.md §7`:

```json
{
  "conformance_version": "4.0",
  "timestamp": "2026-07-11T16:58:00Z",
  "implementation": {
    "name": "Harness Platform",
    "version": "1.0.0",
    "type": "Runtime"
  },
  "compliance_level": "Level 2",
  "summary": { "total_rules": 30, "passed": 30, "failed": 0, "skipped": 0 },
  "results": [
    { "test_id": "TC-01", "status": "PASS", "duration_ms": 12, "message": "..." }
  ]
}
```

---

## Definition of Done — M10

- [ ] 30 test cases implemented
- [ ] Tất cả 30 pass (0 failed)
- [ ] `conformance-report.json` generated và valid theo schema
- [ ] Compliance Level 2 đạt
- [ ] Report committed vào repo

## Review Gate — M10

```
All 30 TC pass
      ↓
conformance-report.json valid
      ↓
Human review report
      ↓
Merge to main
```

---

---

# M11 — Production Hardening

**Milestone:** M11  
**Effort:** 3 ngày  
**Prerequisite:** M10  
**Spec:** `15_SECURITY_MODEL.md`

---

## Objective

Hardening trước khi release: performance, security, observability, stress tests.

---

## Tasks

### T11.1 — Security Validation

Từ `15_SECURITY_MODEL.md`:

- [ ] Path traversal test: `isWithinBoundary` fail cases (30+ test cases)
- [ ] Shared Harness read-only: Runtime không thể ghi `~/.harness/shared/` qua any path
- [ ] Checksum verification: modified file detected
- [ ] `approve()` không accessible qua MCP (negative test)
- [ ] Audit log append-only: không thể delete hoặc modify existing entries

### T11.2 — Performance Benchmarks

Target (từ `09_PLATFORM_SPECIFICATION.md`):

| Operation | Target |
|-----------|--------|
| Context build | < 500ms (100 assets) |
| Capability invocation overhead | < 100ms (excludes actual op) |
| `harness doctor` | < 2s |
| `harness validate` | < 1s |

Run: `vitest bench`

### T11.3 — Memory

- [ ] No memory leak sau 1000 sequential `run()` calls
- [ ] Context cache eviction hoạt động đúng (max 10 entries)
- [ ] Large asset collections không OOM (1000+ assets)

### T11.4 — Concurrency Tests

- [ ] 10 concurrent `run()` calls: không shared state
- [ ] Concurrent governance operations: lock hoạt động đúng
- [ ] Cache race condition: không duplicate compute

### T11.5 — Error Recovery

- [ ] `harness run` recover gracefully sau capability timeout
- [ ] `harness install` cleanup temp files khi interrupted
- [ ] Atomic write: .tmp file cleaned sau failure

### T11.6 — Logging & Observability

- [ ] `HARNESS_LOG_LEVEL=debug` output đủ thông tin để debug
- [ ] Execution logs per task (`logs/execution-{taskId}.jsonl`)
- [ ] Log không chứa secrets, passwords, tokens

### T11.7 — Compatibility Tests

- [ ] Node.js 20, 22 support
- [ ] Linux, macOS, Windows path handling
- [ ] CRLF vs LF line endings xử lý đúng

---

## Definition of Done — M11

- [ ] All security tests pass
- [ ] Performance benchmarks đạt target
- [ ] No memory leaks
- [ ] Concurrency tests pass
- [ ] Logging không leak sensitive data
- [ ] Node.js 20+ và 3 OS support confirmed
- [ ] CHANGELOG.md updated
- [ ] **Human final approval → v1.0.0 tag**

## Review Gate — M11

```
Security tests pass
      ↓
Performance benchmarks meet targets
      ↓
Concurrency tests pass
      ↓
Human final review
      ↓
Tag v1.0.0 → Release
```
