# {{REPO_NAME}} — Agent Instructions

> Generated by harness-os on {{DATE}}. Stack: {{STACK}}.

## Before You Start

1. Run `session_start(".")` to get context and pending tasks
2. Read this file and applicable skills
3. Review last handoff and progress log

## Rules

- **Never claim done without verification** — run `verify_run` first
- **Stay in scope** — check `scope_check` before editing unexpected files
- **Log progress** — use `progress_log` after each meaningful change
- **Handoff at end** — always call `session_handoff` before ending

## Workflow

```
session_start → read instructions → select task → execute → verify → handoff
```

## Project Structure

- `.harness/progress.md` — session history
- `.harness/feature_list.json` — scope boundaries
- `.harness/scope.yaml` — forbidden/allowed paths
- `.harness/verify.yaml` — verification commands
- `.harness/handoff/last.json` — context for next session

## Verification

Run `verify_run(".")` which executes:
- Install dependencies
- Build
- Run tests
- Lint check

All steps must pass before marking a task as done.

## Skills

Use `skill_list()` to see available skills. Key skills:
- `harness-workflow` — the 5-subsystem lifecycle
- `karpathy-guidelines` — Think, Simplicity, Surgical, Goal-Driven
- `verification-loop` — never skip verification
- `search-first` — search before writing
