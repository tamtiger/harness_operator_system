{
  "name": "harness-operator-system",
  "private": true,
  "version": "0.0.1",
  "description": "Universal Coding Harness Monorepo",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,json,md}\""
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "turbo": "^1.11.3",
    "tsup": "^8.0.1",
    "vitest": "^1.1.3",
    "prettier": "^3.1.1",
    "eslint": "^8.56.0"
  }
}